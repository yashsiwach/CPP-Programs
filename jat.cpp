#include<bits/stdc++.h>
using namespace std;
int main(){
    vector<int>jat={2,4,1,5};
    vector<int >temp;
    vector<int>jat2={2,4,1,5};
    if(jat2==temp) cout<<"equal"<<endl;
    else cout<<
    temp=jat;
    for(auto it:temp)
    {
        cout<<it<<" ";
    }
}